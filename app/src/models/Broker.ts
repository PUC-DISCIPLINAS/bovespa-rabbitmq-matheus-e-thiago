export class Broker {
    private _name: string

    constructor (name:string) {
        this._name = name;
    }

    public assignToTopic = (topicName:string[4]) => {

    }

    public createOperation = () => {
        
    }
}